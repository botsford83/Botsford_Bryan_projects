var imageFolder = Ti.Filesystem.getFile(Ti.Filesystem.resourcesDirectory, "images");
var imageFiles = imageFolder.getDirectoryListing();
var imageArray = 0;
var galleryFunction = function(){
	var galleryWindow = Ti.UI.createWindow({
		backgroundColor: "white",
	});
	var galleryImage = Ti.UI.createImageView({
		image: this.image,
		width: 300,
		height: 300,
		top: 40,
	});
	var newImageFunction = function(){
		if(imageArray == 5){
			imageArray = 0;
		}
			else{
				imageArray++;
			}
				galleryImage.image = imageFiles[imageArray];
	};
	newImageButton.addEventListener("click", newImageFunction);
	////
	// Add and open Window
	galleryWindow.add(galleryImage);
	galleryWindow.open();
};
courseLabel.addEventListener("click", galleryFunction);